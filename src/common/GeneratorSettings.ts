"use strict";

class GeneratorSettings{
    canvasWidth = 1000;
    canvasHeight = 1000;

    isStatic = false;

    /* isStatic = true */
    fps = 30;

    /**
     * background color in RGB structure
     */
    backgroundRGB;
    randomColors = false;
}